# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings
pydantic-settings==2.6.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt: passlib 1.7.4 reads bcrypt.__about__ which was removed in bcrypt>=4.1.
# Using 4.0.1 avoids spurious "AttributeError" log warnings on every hash call.
bcrypt==4.0.1

# OpenAI (Whisper + LLM)
openai==1.54.4

# Telegram bot
aiogram==3.14.0
# aiohttp is a transitive dep of aiogram; pin for determinism.
aiohttp==3.10.10

# S3 (Yandex Cloud)
aioboto3==13.2.0

# File upload support in FastAPI
python-multipart==0.0.12

# Async file I/O
aiofiles==24.1.0

# HTTP client (used internally by openai/aioboto3)
httpx==0.27.2
